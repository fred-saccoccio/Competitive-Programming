# Tips

## Neovim snippet
The following code must be added at the end of your init.lua in neovim.
It adds the CpBootstrap command that adds the folowwing C++ code :
```cpp
#include <bits/stdc++.h>
```
